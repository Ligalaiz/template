include ../libs/_layout
  title= 'New Automation Tool'
  body
    p= buildTime + commitHash + version
    +Header
    main.main.main--main
      +Promo
      +WhatDo
      +Project
      +Client
      +Footer('main')
